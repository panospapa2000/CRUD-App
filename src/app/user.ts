export interface Hero {
    id: number;
    firstName: string;
    lastName: string;
    email: string;
    phone: string;
    image: string; // contains a url to an image or a base64 encoding of it
  }